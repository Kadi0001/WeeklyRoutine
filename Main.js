import React from 'react';
import ReactDOM from 'react-dom';
import App from './src/components/first.js'; // Ensure the path is correct

ReactDOM.render(
  <App />,
  document.getElementById('react-app')
);
